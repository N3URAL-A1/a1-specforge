# checkpoint migration

`checkpoint` was personal tooling (Cloud-Brain session-state) and was removed from this public repo in M7.
It now lives as a plain directory at `~/.claude/skills/checkpoint` on the maintainer's machine.
External users have no `checkpoint` skill and can skip it entirely.
