# a1 Gate Registry

**Single source of truth for gate IDs.** Every check that can block or warn in the
a1 pipeline is registered here with a stable slug. Retros reference these IDs
(`gates_fired[].id` — see `learning-schema.md`); a1-evolve computes per-gate ROI
(catches × severity ÷ cost) from those references and can propose retiring
low-value gates, not only adding new ones.

Constitution rules this table enforces:
- **Gates are registered** (invariant 7): a check that can block ships with a registry
  entry, deterministic exit semantics (or an explicit prompt-gate label), and a retro
  attribution id.
- **A gate that cannot fail is documentation** (invariant 8): warning-only checks are
  labelled `warning`, and their real enforcement point (if any) is named in Notes.

**Class:** `deterministic` = CLI/exit-code, `prompt` = LLM agent judgement,
`human` = manual approval. **Cost class:** cheap / med / high.
**Enforcement:** `blocking` = can fail the pipeline · `warning` = advisory only.

| id | phase | class | cost | owning file | enforcement | notes |
|---|---|---|---|---|---|---|
| `gate-c-ac-dryrun` | end of Clarify (pre-Phase-4) | prompt | cheap | a1-new-feature | blocking | NEW (P2). Per FR-AC, narrate the user path against the app's real nav/layout — no build. Catches spec-level blindness. |
| `plan-audit` | Plan | prompt | med | a1-plan (a1-auditor) | blocking | AUDIT.md — plan coverage vs spec ACs. |
| `check-reservations` | Plan / merge | deterministic | cheap | a1-new-feature (Gate 4.5, PASS path) | blocking | NEW (P7), wired M12. `.a1/reservations.json` (migration numbers, route claims): Gate 4.5 claims plan-declared values via `a1-tools check reservations --claim` before dispatching Phase 5 — prevents `parallel_collision`. |
| `lane-split` | Plan (after Pablo) + Execute (before first lane) | deterministic | cheap | a1-plan (`03-plan.md` lane gate), a1-execute (multi-lane step 1) | blocking | NEW (2026-08-05). `a1-tools lane-split check --plan <p>` — exit 0/1/2. Validates a declared `lanes:` block: owns-overlap (reuses `code-scope.cjs` glob math), wave coverage, cross-lane `Depends on:`, cutover-wave-inside-lane. Plans without lanes exit 0 — free for the sequential default. **Check 6 `cross-lane-import` added 2026-09-13, opt-in via `--imports <repo-root>`:** owns-overlap compares declared PATH SETS, so two lanes could own disjoint files and still depend on each other by import — two independent retros named that gap and the auditors, not the gate, found the dependency. Measured why it stayed hidden: the historical case (n3ural-contentbot M4-P2) was PLANNED and never executed, so at plan time there is no code to read. That is also why this gate had fired **15 times with zero catches — it was only ever asked the question it could not answer.** a1-execute re-runs the check before the first lane; there the completed waves' files exist. Without `--imports` the behaviour is unchanged and the JSON now reports `imports_checked: false`, so a PASS cannot be mistaken for "imports are clean". Fixture `_test-fixtures/a1-lane-split/` (5 cases, each mutation-proven). |
| `gate-0-self-report` | Execute (per wave) | deterministic | cheap | a1-execute | blocking | Self-report guard — executor claims cross-checked against artifacts. Added 2026-06/07, no catch data yet. |
| `gate-0.5-surface` | Execute (per wave) | deterministic | med | a1-execute | blocking | Surface-wiring coverage. Same failure class as reconcile (incomplete surface wiring), in-flight. Added 2026-06/07. |
| `gate-0.6-schema` | Execute (per wave) | deterministic | cheap | a1-execute | blocking | Schema/DB-checklist check. Cheap, keep as-is. Added 2026-06/07. |
| `gate-0.7-realpath` | Execute (per wave) | deterministic | cheap | a1-execute | blocking | NEW (P1). Any wave touching SQL / RLS / external-API must show one test run against the real backend (non-mock marker in evidence file). Kills `mock_tests_hide_sql_bugs`. |
| `gate-1-build` | Execute (per wave) | deterministic | med | a1-execute | blocking | Build / type-check must be green. |
| `gate-2-deploy` | Execute (per wave) | deterministic | med | a1-execute | blocking | Deploy/preview succeeds. |
| `gate-3-smoke` | Execute (per wave) | prompt | med | a1-execute | blocking | Per-wave FR-ACs verified live against real URL. Overlaps Phase-6 (P3 de-dup: Phase 6 ✓-references Gate-3 passes). |
| `gate-4.5-fr-consistency` | Execute / standalone | deterministic | cheap | a1-checklist (checks #9/#10, plus #11 in the same call) | blocking | FR-consistency gate: `checklist run <slug>/<feature> --only 9,10,11` (former a1-check, retired M13 — same engine, exit 0/1/2 unchanged). Since 2026-09-26 (spec 010 W7) the same call also runs check #11, registered separately as `spec-roadmap-status-coherent` so its catches are attributed to it. |
| `spec-roadmap-status-coherent` | Phase 4.5 / standalone | deterministic | cheap | a1-checklist (check #11, `_shared/lib/spec-coherence.cjs`) | blocking | NEW (2026-09-26, spec 010-vault-cockpit-contract, Wave 7, FR-028/FR-029). Enforced at a1-new-feature Gate 4.5 (`checklist run <slug>/<feature> --only 9,10,11`) + standalone (`checklist run … --only 11`, `a1-tools product validate --spec-status` for every roadmap feature). Compares `SPEC_TO_ROADMAP_STATUS[spec.status]` with the roadmap feature `status:`: a terminal disagreement (at least one side `done`/`cancelled`) exits 1 and names the reconciling command (`product stage --by <id> --set done` or `spec update-status <path> <status>`); a non-terminal disagreement or an out-of-vocabulary spec status is a warning, an unresolvable spec is `unlinked` (info). Motivated by a measured drift: spec 005 `done` while docs/product/ROADMAP.md said `planned` and recommended it as next (spec 010 F-007) — detected by `product validate --spec-status` on a copy of the live roadmap before reconciliation. No catch data in retros yet. Retro attribution: `{id: spec-roadmap-status-coherent, verdict: pass|fail, caught: <true|false>}`. |
| `phase-6-verify` | Verify | prompt | high | a1-execute (a1-victor-verifier) | blocking | Goal-backward verification vs **spec** ACs. Re-runs only failed/re-touched ACs + cross-wave + edge/SC (P3). |
| `phantom` | Verify (Victor Step 6.5) | deterministic | cheap | a1-phantom | warning | CLI always exits 0 (standalone). Enforcement point (P4): PHANTOM verdicts on non-`# no-code` tasks become BLOCKER findings inside Victor's VERIFICATION.md. |
| `isolation-gate` | before Phase 5 Implement (a1-new-feature) / before any code change (a1-fix) | human | cheap | a1-new-feature (SKILL.md "Isolation Gate") + a1-fix (SKILL.md "Isolation Gate") | blocking | NEW (2026-09-11), registered after the fact: the gate has been a HARD RULE in both skills for months and retros already attributed to this id twice, but no row existed — so a1-evolve discarded those attributions silently (invariant 7 breach by omission, found while planning spec 007). No feature code in the primary checkout: work moves to a git worktree on a branch off `main`. Legitimate exception, declared at plan time and not deviated at execute time: phases whose write targets are external live symlinked repos (a worktree copy would miss the live symlinks) — see `pattern/a1-learnings/a1-execute.md` 2026-08. |
| `no-imported-expectations` | continuous (CI, project repos) | deterministic | cheap | `scripts/check-no-imported-expectations.mjs` (n3ural-contentbot) | blocking | NEW (2026-09-13, spec 008). `npm run check:no-imported-expectations` — AST check (TypeScript compiler API) over test files: exit 0 clean / 1 finding(s) / 2 usage-or-parse error. Flags an assertion whose EXPECTED value is imported from the same module as the function under test — class 4 of `_shared/agent-lessons.md#theo-mutation-question`, where the expectation moves with its source. **Has catch data:** mutation-verified against the historical `MAX_REASON_LENGTH` case recovered verbatim from git (a5994b4), and reports **11 pre-existing false-greens** in n3ural-contentbot as of 2026-09-13, each confirmed by mutation (e.g. `HEARTBEAT_STATE_SCHEMA_VERSION` 1→999 leaves all 25 tests of its own suite green). Enum-member comparisons (`toBe(STATUS.MERGED)`) were measured NOT to be this class and are excluded rather than reported as 125 false alarms. FR-002 exception list holds exactly one named entry with its reason. Retro attribution: `{id: no-imported-expectations, ...}`. |
| `branch-coverage` | continuous (CI, project repos) | deterministic | med | `scripts/check-branch-coverage.mjs` (n3ural-contentbot) | blocking | NEW (2026-09-13, spec 008). `npm run check:branch-coverage` — exit 0 all named modules at or above threshold / 1 a module below it or missing from the report / 2 the coverage run itself failed. Scope is a NAMED list of four critical modules, never a global threshold (a global number over 104 test files is one nobody acts on). **Threshold 75% is a ratchet DERIVED from a 2026-09-13 baseline measurement** (state.core.mjs 75.67%, operator-alert 94.28%, health 94.44%, resolve-secrets 100%), not an aspiration — a gate red on its first run gets switched off. **Has catch data:** raising the threshold to 90 exits 1, and deleting the state machine's own test file drops it 75.67%→66.66% and exits 1. Requires `@vitest/coverage-v8@3.2.7` (pinned to the repo's vitest 3.2.7; npm resolves 5.0.0 by default, which demands vitest 5). Retro attribution: `{id: branch-coverage, ...}`. |
| `phase-artifacts-exist` | Verify (no-code phases only) | deterministic | cheap | `skills/a1-execute/workflows/03-verify.md` Step 0 (trigger field owned by `agents/a1-pablo-planner.md`, waiver rule by `_shared/parallel-spec-isolation.md` R1 exception 2) | blocking | NEW (2026-09-20, 7th a1-evolve run). Replaces `gate-1-build` for phases declaring `phase_kind: no-code`, whose write targets lie outside the repo (vault notes, docs prose, external channels) and which therefore produce nothing a build or type-check could green-light. Checks the wave's plan-declared artifact paths: each must exist AND satisfy one content assertion named in the plan — existence alone is a gate that cannot fail (invariant 8). Motivated by 4 measured phases (n3ural-socialmedia M1-P1..P3, ai-server, 2026-09-16/17) that were pushed through an isolation gate with nothing to isolate while `gate-1-build` stayed unsatisfiable. Wired 2026-09-20 in the same commit: `phase_kind: <code|no-code>` is a required field in Pablo's PLAN frontmatter template, and `03-verify.md` Step 0 branches on it — a missing value reads as `code`, never as a waiver. No catch data yet. Retro attribution: `{id: phase-artifacts-exist, verdict: pass|fail, caught: <true|false>}` per `skills/a1-execute/workflows/03-verify.md` Step 0. |
| `evolve-proposal-confirm` | a1-evolve Phase 4 (Apply) | human | cheap | a1-evolve (`04-apply.md` §4b) | blocking | NEW (2026-09-20, registered after the fact). The per-proposal confirmation before any diff is written — a1-evolve never edits a skill or agent file unattended. Flagged as `own_gate_unregistered` in the 2026-08-27 run and still unregistered two runs later, so its attributions were being discarded silently by a1-evolve's own ROI step (invariant 7 breach by omission, the exact class this skill reported about other skills). Retro attribution: `{id: evolve-proposal-confirm, verdict: pass, caught: <true if the user rejected or edited a proposal>}`. |
| `collect-roots` | a1-evolve Phase 1 (Collect) | deterministic | cheap | a1-evolve (`01-collect.md` §1a) | blocking | NEW (2026-09-11). `a1-tools learnings roots` — exit 0 roots / 2 bad `A1_CODE_ROOTS` / 3 nothing resolved. Guards the precondition of every synthesis: a collect phase whose globs match nothing must abort, not report "0 new entries". Added after the hardcoded `~/code` glob would have made the 6th run collect nothing while reporting success. Retro attribution: `{id: collect-roots, ...}` per `skills/a1-evolve/workflows/04-apply.md` Retro block. |
| `fix-integrity` | a1-fix | deterministic | cheap | a1-fix | blocking | Integrity-check / postmortem retro-integrity. Cheap, deterministic, keep as-is. |
| `checklist-preflight` | Pre-feature | prompt | med | a1-checklist | blocking | Launch/pre-feature readiness gate. |
| `reconcile-probe` | Post-hoc | deterministic | med | a1-reconcile | warning | Spec-drift / incomplete surface wiring, post-hoc (vs gate-0.5 in-flight). |
| `pr-reinhard` | Pre-merge | prompt | high | a1-pr-review (reinhard) | blocking | Diff-level review, pre-merge only (P6 scope). |
| `modernize-parity` | Modernize | deterministic | med | a1-modernize | blocking | Behaviour-parity check. |
| `modernize-g1..g6` | Modernize | human | high | a1-modernize | blocking | Six human approval gates (collapsed). Keep; auto-approval precedent needs an audit trail (FMEA-5). |
| `quick-eligibility` | entry (pre-Discover / pre-Pre-Flight) | deterministic | cheap | a1-quick / a1-new-feature+a1-fix | blocking | NEW (spec 004-xs-quick-lane, Wave 1). `quick eligibility` decides XS-lane admission (intent/file/diff budget, forbidden surface, clean tree, no reservation conflict) — no LLM, no network. |
| `quick-escalation` | exit (mid-run tripwire) | deterministic | cheap | a1-quick | blocking | NEW (spec 004-xs-quick-lane). Mid-run tripwire hard-stop that hands an in-progress quick run off to the full pipeline as a seed artifact. |
| `pre-flight-github-scope` | Phase 1 (pre-build) | deterministic | cheap | proorc-release | blocking | NEW (M11-P3, 2026-08-22). Checks `gh auth status` active account matches the repo owner (`n3urala1-rob`) and that `.github/workflows/release.yml` declares `permissions: contents: write` — **not** a `delete_repo`-scope check (that belongs to ProOrc's unrelated in-app repo-deletion feature). No catch data yet. |
| `version-consistency-gate` | Phase 2 (post-extraction) | deterministic | cheap | proorc-release | blocking | NEW (M11-P3, 2026-08-22). Validates `pubspec.yaml`-extracted version matches semver `X.Y.Z` before it is used for any build/tag/DMG-naming step. No catch data yet. |
| `tag-milestone-confirmation` | Phase 1 (pre-build) | human | cheap | proorc-release | blocking | NEW (M11-P3, 2026-08-22). Confirms with the user that a tag is milestone-worthy before tagging, per the "only tag bundled milestones" convention (CLAUDE.md). Declining routes to `proorc-quick-install` instead. No catch data yet. |
| `roadmap-gate` | Phase 0 (pre-Discover) | deterministic | cheap | a1-new-feature (`workflows/00-roadmap-gate.md`) | blocking | Registry backfill 2026-08-27: active since the skill's introduction and referenced in retros, but never registered (invariant 7). |
| `scope-claim` | Phase 4.5 / a1-fix Pre-Flight | deterministic | cheap | `_shared/parallel-spec-isolation.md` (a1-new-feature, a1-fix) | blocking | Registry backfill 2026-08-27. Scope-claim ordering of the parallel-spec-isolation convention. |
| `stop-gate-human` | Execute (plan-declared) | human | cheap | a1-execute | blocking | Registry backfill 2026-08-27. Generic pattern: a plan-declared STOP point for a decision the executor must not make alone. 1 catch (M11-P1 RETIRE decision). |
| `backup-gate-human` | Execute (Wave 0) | human | cheap | a1-execute | blocking | Registry backfill 2026-08-27. Backup confirmation before writing into live targets with no VCS safety net. |
| `settings-json-diff-gate-human` | Execute (config-touching wave) | human | cheap | a1-execute | blocking | Registry backfill 2026-08-27. Diff approval before edits to session-critical config (settings.json hooks). |
| `review-empirical-probes` | Pre-merge review | prompt | med | a1-reinhard-reviewer | blocking | Registry backfill 2026-08-27. Empirical probe requirement for high-blast-radius code — 3 catches in 3 runs, see `agent-lessons.md#reinhard-empirical-probes`. |
| `retro-gate-ids` | Retro (write time, all learning-enabled skills) | deterministic | cheap | `_shared/retro-template.md` (validator: `_shared/lib/retro-validate.cjs`) | blocking | NEW (2026-09-12, spec 007-retro-gate-id-validator, Wave 2). `a1-tools retro validate <retro-path>` — exit 0 all `gates_fired[].id` registered (or field absent, legitimate for read-only reporters), 1 at least one drift/unknown id OR a bad argument (shared `usage()` convention — tell them apart by stdout: a real run emits the JSON report, a usage error does not), 2 missing-file/unreadable-registry/malformed-block. Measured 2026-09-12: 11 of 58 corpus entries were drift across 3 spellings before this gate existed. Retro attribution: `{id: retro-gate-ids, verdict: pass, caught: <true|false>}`. |
| `workflow-pipeline-exit` | CI / pre-merge | deterministic | cheap | `_shared/lib/workflow-lint.cjs` | blocking | NEW (2026-09-12, spec 007-retro-gate-id-validator, Wave 3). `a1-tools workflow lint [--root <path>]` — scans `skills/*/workflows/*.md` (fenced ```bash blocks only) for a pipeline whose STATUS is tested where `$?`/`||` actually reads the parser's exit code, not the piped command's. Exit 0 clean / 1 at least one finding OR an unrecognised flag / 2 hostile-or-unreadable `--root`. Two predicates (status-testing vs value-defaulting, see module header) so a `grep -c ... \|\| echo 0` value-default shape is not a false positive — though measured 2026-09-12 that predicate has NO live true positive here and the line originally cited as its motivation (`03-verify.md:140`) contains no shell pipe at all, so it is forward-looking rather than currently load-bearing. No exception-list marker shipped — SC-004: the live repo produced no false positive the two predicates could not separate, so an unused `<!-- workflow-lint: value-default -->` mechanism was left out per the wave brief. Enforcement point is real as of 2026-09-12: `.github/workflows/test.yml` runs `workflow lint` as its own step (before that, nothing outside the fixtures invoked it — invariant 8 breach found in review). No catch data yet; RED proof is fixture-based (`_test-fixtures/a1-workflow-lint/snippets/`) — the live repo has zero true positives today (the 2026-09-11 defect was fixed the same morning), same honest absence-of-catches convention as `lane-split` and `collect-roots`. Retro attribution: `{id: workflow-pipeline-exit, verdict: pass, caught: <true|false>}`. |
| `plan-review-xprov` | Plan 4b | prompt | med | a1-plan (workflows/04b-xprov-review.md) | warning | NEW (2026-09-24, spec 009-cross-provider-review-gate). Cross-provider review of the phase's PLAN.md: after AUDIT.md carries PASS, Phase 4b sends the plan to Codex through the vendored claudex-loop runner (`a1-tools xprov`, mode `review`) and normalizes the result into the findings schema Reinhard already produces; every path that is not a validated APPROVED maps to `fail`. **Ships as `warning`; flips to `blocking` in the commit that records one live review and one live inspect in the ADR §6 while `_test-fixtures/a1-xprov/run-tests.sh` is green in CI** (`docs/adr/2026-09-24-cross-provider-review-gate.md`). Enforcement point while `warning` (invariant 8): a1-plan Phase 4b itself — a `fail` prints a warning block naming the reason, the pipeline continues, and the retro records `verdict: fail`; a1-execute's Load check additionally warns when no `xreview/index.json` entry with `verdict: pass` matches the current PLAN.md's sha256. No catch data yet. Retro attribution: `{id: plan-review-xprov, verdict: pass|fail, caught: <bool>}`. |
| `wave-inspect-xprov` | Execute (per wave, step 2b-x) | prompt | med | a1-execute (workflows/02-execute.md step 2b-x) | warning | NEW (2026-09-24, spec 009-cross-provider-review-gate). Cross-provider inspection of each wave's diff: after the commit-landed gate in step 2b and before the checkpoint in step 2c, a1-execute sends the wave diff (`--mode inspect --base <PRE_WAVE_HEAD>`) to Codex through the same vendored runner and normalizes the result the same way; once per wave, per lane wave in multi-lane phases. **Ships as `warning`; flips to `blocking` in the commit that records one live review and one live inspect in the ADR §6 while `_test-fixtures/a1-xprov/run-tests.sh` is green in CI** (same commit as `plan-review-xprov`). Enforcement point while `warning` (invariant 8): a1-execute step 2b-x — a `fail` prints a warning block naming the reason, the wave proceeds to its checkpoint, and the retro records `verdict: fail`; once `blocking`, Phase 3 (Victor) is not spawned while any completed wave lacks an `index.json` entry with `verdict: pass` or `waived: true`. No catch data yet. Retro attribution: `{id: wave-inspect-xprov, verdict: pass|fail, caught: <bool>}`. |


**Alias warning — ids are copied verbatim, never paraphrased.** a1-evolve's
gate-ROI step silently discards any `gates_fired[].id` absent from the table
above, so an invented id looks like data and counts as nothing. Observed
drifts (2026-08-27 synthesis, 8 of 12 ids in the corpus were unregistered):

- written `lane-split-check` → correct id is `lane-split`
- written `consistency-gate-4-5` → correct id is `gate-4.5-fr-consistency`
- written `full-regression-gate` → correct id is `gate-1-build` (full-suite variant)

**This warning demonstrably does not hold on its own.** It was written on
2026-08-27 naming `lane-split-check` explicitly; between then and 2026-09-11
that exact misspelling was written **seven more times**, plus
`consistency-gate-4-5` once and `full-regression-gate` once — 9 of 33
`gates_fired` entries in the corpus. Measured effect: `lane-split` counted 7
firings instead of 14, which is the difference between "not enough data" and a
retirement-candidate verdict. Prose in three places (here, `retro-template.md`,
and `02-cluster.md`) has now failed twice, so the fix has to be machine-checked
— tracked as spec `007-retro-gate-id-validator` in the learning store
(`$A1_VAULT_ROOT/project/a1-specforge/spec/`, alongside specs 001–006), not as
another paragraph here.

(Deliberately a bullet list, not a table: a markdown table here would share the
id-table's row shape, and any parser scraping `^| \`id\`` would then accept the
wrong-hand column as a valid id — the exact drift this section exists to stop.)

If the gate you ran has no row here, add one in the same commit as the retro
(invariant 7) rather than inventing a slug.
