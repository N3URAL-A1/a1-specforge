# a1-pablo-planner (Sub-Agent Reference)

This is **not** an agent definition. It is a pointer.

## Source of truth

```
~/.claude/agents/a1-pablo-planner.md
```

## Usage in a1-modernize

Spawned in Phase 5 (Plan) to decompose gap findings and approved proposals
into an executable wave plan (PLAN.md). Each wave gets: goal, FR-ACs,
deployment chain, test requirement, rollback, dependencies.
