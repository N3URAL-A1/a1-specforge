# a1-erik-executor (Sub-Agent Reference)

This is **not** an agent definition. It is a pointer.

## Source of truth

```
~/.claude/agents/a1-erik-executor.md
```

## Usage in a1-modernize

Spawned per wave in Phase 6 (Execute) after Theo's test skeleton is ready.
Erik implements the wave and fills in the test skeleton. Commits with
`feat(modernize/<slug>): W-NN <title>`.
