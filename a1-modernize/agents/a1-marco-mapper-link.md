# a1-marco-mapper (Sub-Agent Reference)

This is **not** an agent definition. It is a pointer.

## Source of truth

```
~/.claude/agents/a1-marco-mapper.md
```

## Usage in a1-modernize

Spawned in Phase 2 (Reverse-Spec) in parallel with (or just before) a1-rafael-reverse-spec.
Marco maps the code structure (arch + tech focus). His MAP.md is the structural
input that Rafael uses for behavior extraction.
